# 依赖包 (非常大，不需要上传)
node_modules
client/node_modules

# 构建输出
dist
client/dist

# 环境变量 (保护隐私)
.env

# Prisma 生成的文件 (如果有)
prisma/migrations

# 系统缓存
.DS_Store
Thumbs.db

# 你的本地数据库 (如果你用的是 SQLite)
*.db
*.db-journal

# VS Code 配置 (可选)
.vscode