{
  "name": "medianas-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "install:all": "pnpm install"
  },
  "devDependencies": {
    "turbo": "^1.10.0" 
  }
}